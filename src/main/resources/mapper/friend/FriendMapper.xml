<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.crown.friend.mapper.FriendMapper">

    <resultMap id="friendResultMap" type="com.crown.friend.dto.FriendDto">
        <result property="memberId"   column="member_id"/>
        <result property="nickname"   column="nickname"/>
        <result property="profileImg" column="profile_img"/>
        <result property="score"      column="score"/>
        <result property="status"     column="status"/>
    </resultMap>

    <insert id="insertRequest">
        INSERT INTO friend (requester_id, receiver_id, status)
        VALUES (#{requesterId}, #{receiverId}, 'PENDING')
    </insert>

    <update id="updateStatus">
        UPDATE friend
        SET status = #{status}
        WHERE requester_id = #{requesterId} AND receiver_id = #{receiverId}
    </update>

    <delete id="deleteByPair">
        DELETE FROM friend
        WHERE (requester_id = #{userId} AND receiver_id = #{friendId})
           OR (requester_id = #{friendId} AND receiver_id = #{userId})
    </delete>

    <!-- 수락된 친구 목록: 내가 requester 또는 receiver인 ACCEPTED 관계 -->
    <select id="findFriends" resultMap="friendResultMap">
        SELECT
            m.member_id,
            m.nickname,
            m.profile_img,
            m.score,
            f.status
        FROM friend f
        JOIN member m ON m.member_id = CASE
            WHEN f.requester_id = #{memberId} THEN f.receiver_id
            ELSE f.requester_id
        END
        WHERE (f.requester_id = #{memberId} OR f.receiver_id = #{memberId})
          AND f.status = 'ACCEPTED'
        ORDER BY m.nickname
    </select>

    <!-- 나에게 온 대기 중 요청 -->
    <select id="findPendingRequests" resultMap="friendResultMap">
        SELECT
            m.member_id,
            m.nickname,
            m.profile_img,
            m.score,
            f.status
        FROM friend f
        JOIN member m ON m.member_id = f.requester_id
        WHERE f.receiver_id = #{memberId}
          AND f.status = 'PENDING'
        ORDER BY f.created_at DESC
    </select>

    <select id="findByPair" resultMap="friendResultMap">
        SELECT
            m.member_id,
            m.nickname,
            m.profile_img,
            m.score,
            f.status
        FROM friend f
        JOIN member m ON m.member_id = f.receiver_id
        WHERE f.requester_id = #{requesterId} AND f.receiver_id = #{receiverId}
    </select>

</mapper>
